diff --git a/src/effects.cpp b/src/effects.cpp
index 6e04585..2738399 100644
--- a/src/effects.cpp
+++ b/src/effects.cpp
@@ -986,6 +986,16 @@ QPoint EffectsHandlerImpl::desktopGridCoords(int id) const
     return VirtualDesktopManager::self()->grid().gridCoords(id);
 }
 
+bool EffectsHandlerImpl::isSpatialMode() const
+{
+    return VirtualDesktopManager::self()->isSpatialMode();
+}
+
+bool EffectsHandlerImpl::desktopExists(int row, int col) const
+{
+    return VirtualDesktopManager::self()->grid().at(QPoint(col, row)) != nullptr;
+}
+
 QPoint EffectsHandlerImpl::desktopCoords(int id) const
 {
     QPoint coords = VirtualDesktopManager::self()->grid().gridCoords(id);
diff --git a/src/effects.h b/src/effects.h
index 9dfa35f..4d7bc05 100644
--- a/src/effects.h
+++ b/src/effects.h
@@ -92,6 +92,8 @@ public:
     int workspaceHeight() const override;
     int desktopAtCoords(QPoint coords) const override;
     QPoint desktopGridCoords(int id) const override;
+    bool isSpatialMode() const override;
+    bool desktopExists(int row, int col) const override;
     QPoint desktopCoords(int id) const override;
     int desktopAbove(int desktop = 0, bool wrap = true) const override;
     int desktopToRight(int desktop = 0, bool wrap = true) const override;
diff --git a/src/libkwineffects/kwineffects.h b/src/libkwineffects/kwineffects.h
index 3e34fa8..7c79bfe 100644
--- a/src/libkwineffects/kwineffects.h
+++ b/src/libkwineffects/kwineffects.h
@@ -997,6 +997,19 @@ public:
      * @returns The coords of desktop @a id in grid units.
      */
     virtual QPoint desktopGridCoords(int id) const = 0;
+    /**
+     * @returns Whether the virtual desktop manager is in spatial mode.
+     * In spatial mode, navigation between desktops uses the spatial neighbor
+     * map rather than the regular grid layout, allowing sparse grid arrangements.
+     */
+    virtual bool isSpatialMode() const = 0;
+    /**
+     * @returns Whether a desktop exists at the given grid position @a row, @a col.
+     * In sparse spatial layouts some grid positions may be vacant.
+     * @param row The zero-based row index in the desktop grid.
+     * @param col The zero-based column index in the desktop grid.
+     */
+    virtual bool desktopExists(int row, int col) const = 0;
     /**
      * @returns The coords of the top-left corner of desktop @a id in pixels.
      */
